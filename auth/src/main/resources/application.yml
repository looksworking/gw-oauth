spring:
  profiles:
    active: debug
logging:
  level:
    root: INFO
    org.springframework.web: TRACE
    org.springframework.security: TRACE
    org.springframework.security.oauth2: TRACE

#  datasource:
#    url: jdbc:postgresql://gw-oauth-database:5432/auth?currentSchema=public
#    username: postgres
#    password: postgres
#    driver-class-name: org.postgresql.Driver
#
#  liquibase:
#    change-log: classpath:/liquibase/changelog.yml

#server:
#  servlet:
#    context-path: /auth

